- implement custom columns to apply to the matrix (Koreans suggestion)
- See if you can implement generating functions on a separate page
- See if you can implement Sage via Lambda with Docker containers
- Add a button to reveal full sequence in one-click
- Fix auth flow for password reset
- Compute and display anti-diagonal
- Enter the sequence from generating functions (direct from that)
- dynamically fetch if anything is an OEIS sequence on results side
- tweedle functionality

